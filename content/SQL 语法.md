- 注释
    - -      # 单行注释
    - /**/    # 多行注释
- 单引号
    - 字符串大小写敏感
- 双引号
    - 转日期时, 双引号直接显示。to_char(sysdate,'yyyy "年" mm "月" dd "日" day')
- 表别名不加as关键字, 有别名后原名无效
- oracle
    - ||      # 管道符号
        - select ename || '的薪水是' || sal from emp;
