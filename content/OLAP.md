- OnLine Analytice Processing
- 特点
    - 只读取几列
- 与OLTP对比
    - {{table}}
        - 属性
            - OLTP
                - OLAP
        - 主要读取模式
            - 查询少，按键读取
                - 大批量记录上聚合
        - 主要写入模式
            - 随机访问，写入要求低延时
                - 批量导入(ETL)，事件流
        - 主要用户
            - 终端用户
                - 数据分析师，决策支持
        - 处理的数据
            - 数据的最新状态
                - 随时间推移的历史事件
        - 数据集尺寸
            - GB~TB
                - TB~PB
